---
phase: 15-signal-reflexes
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/soma/hooks/pre_tool_use.py
  - src/soma/hooks/notification.py
  - src/soma/hooks/common.py
  - src/soma/report.py
  - tests/test_signal_reflex_hooks.py
autonomous: true
requirements: [RFX-05, RFX-06, RFX-07, RFX-08, RFX-09]

must_haves:
  truths:
    - "Commit gate blocks git commit on quality grade D/F in PreToolUse"
    - "Signal reflex injections appear in notification output"
    - "Auto-checkpoint executes git stash push in reflex mode"
    - "Session report includes reflex stats section"
    - "Signal reflex events are audit-logged"
    - "Max 2 signal injections per notification cycle"
  artifacts:
    - path: "src/soma/hooks/pre_tool_use.py"
      provides: "Commit gate integration"
      contains: "evaluate_commit_gate"
    - path: "src/soma/hooks/notification.py"
      provides: "Signal reflex injection integration"
      contains: "evaluate_all_signals"
    - path: "src/soma/hooks/common.py"
      provides: "Auto-checkpoint helper + checkpoint counter"
      contains: "_auto_checkpoint"
    - path: "src/soma/report.py"
      provides: "Reflex stats in session report"
      contains: "Reflex"
    - path: "tests/test_signal_reflex_hooks.py"
      provides: "Integration tests for hook wiring"
      min_lines: 80
  key_links:
    - from: "src/soma/hooks/pre_tool_use.py"
      to: "src/soma/signal_reflexes.py"
      via: "imports evaluate_commit_gate"
      pattern: "from soma\\.signal_reflexes import evaluate_commit_gate"
    - from: "src/soma/hooks/notification.py"
      to: "src/soma/signal_reflexes.py"
      via: "imports evaluate_all_signals"
      pattern: "from soma\\.signal_reflexes import"
    - from: "src/soma/hooks/common.py"
      to: "subprocess"
      via: "git stash push execution"
      pattern: "subprocess\\.run.*git.*stash"
---

<objective>
Wire signal reflexes into the hook layer (pre_tool_use for commit gate, notification for injections) and extend session reports with reflex stats.

Purpose: Connect Plan 01's pure evaluators to the real SOMA hook pipeline. The commit gate blocks in PreToolUse, signal injections appear in Notification, git stash runs as a side effect in common.py, and session reports show reflex stats.
Output: Modified hooks + report + integration tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-signal-reflexes/15-CONTEXT.md
@.planning/phases/15-signal-reflexes/15-RESEARCH.md
@.planning/phases/15-signal-reflexes/15-01-SUMMARY.md

@src/soma/hooks/pre_tool_use.py
@src/soma/hooks/notification.py
@src/soma/hooks/common.py
@src/soma/report.py
@src/soma/signal_reflexes.py

<interfaces>
<!-- From Plan 01 output — signal_reflexes.py exports -->
From src/soma/signal_reflexes.py:
```python
def evaluate_predictor_checkpoint(prediction, soma_mode) -> ReflexResult: ...
def evaluate_drift_guardian(drift, original_task, current_activity) -> ReflexResult: ...
def evaluate_handoff(success_rate, handoff_text, agent_id) -> ReflexResult: ...
def evaluate_rca_injection(error_rate, rca_text) -> ReflexResult: ...
def evaluate_commit_gate(grade, tool_name, tool_input) -> ReflexResult: ...
def evaluate_all_signals(...) -> list[ReflexResult]: ...
```

From src/soma/reflexes.py:
```python
@dataclass(frozen=True, slots=True)
class ReflexResult:
    allow: bool
    reflex_kind: str = ""
    block_message: str | None = None
    inject_message: str | None = None
    detail: str = ""
```

From src/soma/hooks/common.py:
```python
def get_soma_mode() -> str: ...
def get_engine() -> tuple: ...
def read_action_log(agent_id: str = "") -> list[dict]: ...
def get_block_count(agent_id: str = "") -> int: ...
def increment_block_count(agent_id: str = "") -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook integration — commit gate + signal injections + git stash helper</name>
  <files>src/soma/hooks/pre_tool_use.py, src/soma/hooks/notification.py, src/soma/hooks/common.py, tests/test_signal_reflex_hooks.py</files>
  <read_first>
    - src/soma/hooks/pre_tool_use.py (current reflex flow, where to add commit gate)
    - src/soma/hooks/notification.py (current notification flow, where to add signal injections)
    - src/soma/hooks/common.py (existing helpers, block_count, soma_mode)
    - src/soma/signal_reflexes.py (Plan 01 output — the evaluator functions)
    - src/soma/hooks/post_tool_use.py (how predictor/quality/task tracker are loaded)
    - tests/test_reflex_hooks.py (existing hook test patterns)
  </read_first>
  <action>
    **common.py additions:**
    1. Add `_auto_checkpoint(checkpoint_number: int) -> bool` helper (per research Pattern 4):
       - Check `subprocess.run(["git", "rev-parse", "--git-dir"], ...)` first (per pitfall 1)
       - If not git repo, return False silently
       - Run `subprocess.run(["git", "stash", "push", "-m", f"soma-checkpoint-{checkpoint_number}"], capture_output=True, text=True, timeout=10)`
       - Return True on success, False on failure/timeout
    2. Add `get_checkpoint_count(agent_id) -> int` and `increment_checkpoint_count(agent_id) -> int` — counter persisted in `~/.soma/sessions/{agent_id}/checkpoint_count` (same pattern as block_count)

    **pre_tool_use.py modifications:**
    1. After the existing reflex block (line ~89) and before the guidance section (line ~92), add commit gate check:
       - Only in reflex OR guide mode (not observe)
       - Import `evaluate_commit_gate` from `soma.signal_reflexes`
       - Load quality grade: `from soma.state import get_quality_tracker; qt = get_quality_tracker(); report = qt.get_report(); grade = report.grade`
       - Call `evaluate_commit_gate(grade, tool_name, tool_input)`
       - If not allow: audit log (mode='reflex', type='reflex', reflex_kind='commit_gate'), increment_block_count, print block_message to stderr, sys.exit(2) — per D-13
       - If inject_message (grade C warning per D-14): print to stderr

    **notification.py modifications:**
    1. After the findings section (after line ~131, before output), add signal reflex evaluation:
       - Only when soma_mode in ("guide", "reflex") and actions >= 3 (respect grace period)
       - Gather signal inputs:
         - `prediction`: load predictor via `get_predictor(agent_id)`, call `predictor.predict(next_boundary)` using same pattern as post_tool_use.py
         - `success_rate`: `from soma.halflife import predict_success_rate; predict_success_rate(actions, half_life)`
         - `handoff_text`: `from soma.halflife import generate_handoff_suggestion; generate_handoff_suggestion(agent_id, actions, half_life, success_rate)`
         - `original_task` + `current_activity`: from task_tracker get_context() (already loaded above)
         - `error_rate`: from vitals dict
         - `rca_text`: `from soma.rca import diagnose; diagnose(action_log, vitals, pressure, level_name, actions)` only when error_rate > 0.3
       - Call `evaluate_all_signals(...)` with gathered inputs
       - For each result with inject_message: append to lines
       - For predictor_checkpoint result in reflex mode: call `_auto_checkpoint` from common.py, `increment_checkpoint_count`
       - Per D-09: if handoff result fires, reduce trust weight: `engine._graph.update_edge(agent_id, ..., trust * success_rate)` — only if agent has graph edges
       - Wrap ALL signal evaluation in try/except (never crash notification hook)
       - Per D-17: audit log each signal reflex event via AuditLogger.append(mode='reflex', type='reflex', reflex_kind=result.reflex_kind)

    **tests/test_signal_reflex_hooks.py:**
    - Test _auto_checkpoint: mock subprocess.run, verify git rev-parse check, verify stash command
    - Test commit gate integration path: mock get_quality_tracker returning grade D, verify sys.exit(2)
    - Test notification signal injection: mock evaluators, verify inject_messages appear in output
    - Test signal injection capped at 2 per cycle
    - Test observe mode skips signal reflexes
  </action>
  <verify>
    <automated>cd /Users/timur/projectos/SOMA && python -m pytest tests/test_signal_reflex_hooks.py tests/test_signal_reflexes.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep "evaluate_commit_gate" src/soma/hooks/pre_tool_use.py succeeds
    - grep "evaluate_all_signals\|signal_reflexes" src/soma/hooks/notification.py succeeds
    - grep "_auto_checkpoint" src/soma/hooks/common.py succeeds
    - grep "git.*stash" src/soma/hooks/common.py succeeds
    - grep "checkpoint_count" src/soma/hooks/common.py succeeds
    - grep "AuditLogger\|audit" src/soma/hooks/notification.py succeeds (D-17)
    - python -m pytest tests/test_signal_reflex_hooks.py -x passes
  </acceptance_criteria>
  <done>Commit gate blocks git commit on grade D/F in PreToolUse. Signal injections appear in notification. Auto-checkpoint helper runs git stash. Audit logging for signal reflex events. All hook integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend session report with reflex stats</name>
  <files>src/soma/report.py, tests/test_report.py</files>
  <read_first>
    - src/soma/report.py (current report structure, where to add section)
    - tests/test_report.py (existing report test patterns)
    - src/soma/hooks/common.py (get_block_count, get_checkpoint_count for stats)
  </read_first>
  <action>
    Per D-16: Add a "## Reflexes" section to `generate_session_report()` between the Interventions and Cost sections.

    The section includes:
    - **Blocks:** total block count (from `get_block_count(agent_id)` — includes both pattern and signal reflex blocks)
    - **Checkpoints:** checkpoint count (from `get_checkpoint_count(agent_id)`)
    - **Top reflex:** most common reflex_kind from audit log. Read audit log entries where type='reflex', count by reflex_kind, show top one. Use `from soma.audit import AuditLogger; logger = AuditLogger(); entries = logger.read()` if available, otherwise skip gracefully.
    - **Estimated errors prevented:** block_count (each block prevented a potential error)

    Format:
    ```
    ## Reflexes

    - **Blocks:** {N}
    - **Checkpoints:** {M}
    - **Top reflex:** {kind} ({count} triggers)
    - **Estimated errors prevented:** {N}
    ```

    If no reflex activity (blocks=0, checkpoints=0), show "No reflex activity this session."

    Wrap in try/except — report generation must never fail due to missing reflex data.

    Update existing report tests to verify the new section appears.
  </action>
  <verify>
    <automated>cd /Users/timur/projectos/SOMA && python -m pytest tests/test_report.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep "Reflexes" src/soma/report.py succeeds
    - grep "Blocks" src/soma/report.py succeeds
    - grep "Checkpoints" src/soma/report.py succeeds
    - grep "Top reflex" src/soma/report.py succeeds
    - grep "Estimated errors prevented" src/soma/report.py succeeds
    - python -m pytest tests/test_report.py -x passes
  </acceptance_criteria>
  <done>Session report includes Reflexes section with blocks, checkpoints, top reflex, and estimated errors prevented. All report tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_signal_reflex_hooks.py tests/test_signal_reflexes.py tests/test_report.py -x -v` — all related tests pass
- `python -m pytest tests/ -x --timeout=30` — full suite green
- `ruff check src/soma/hooks/pre_tool_use.py src/soma/hooks/notification.py src/soma/hooks/common.py src/soma/report.py` — no lint errors
</verification>

<success_criteria>
- Commit gate blocks git commit on grade D/F, warns on C
- Signal injections (predictor, drift, handoff, RCA) appear in notification output
- Auto-checkpoint runs git stash in reflex mode
- Max 2 signal injections per notification cycle
- Session report shows reflex stats
- Audit log captures signal reflex events
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-signal-reflexes/15-02-SUMMARY.md`
</output>

---
phase: 12-contributing-guide
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - demo.tape
  - README.md
autonomous: false
requirements: [DEMO-01]

must_haves:
  truths:
    - "A VHS tape file exists that scripts a terminal demo of SOMA"
    - "The tape demonstrates install, setup, and monitoring in action"
    - "README.md references the demo output"
  artifacts:
    - path: "demo.tape"
      provides: "VHS tape script for terminal recording"
      contains: "Output"
    - path: "README.md"
      provides: "Updated README with demo reference"
      contains: "demo"
  key_links:
    - from: "README.md"
      to: "demo.tape"
      via: "references the generated demo GIF/output"
      pattern: "demo"
---

<objective>
Create a demo tape file for terminal recording (DEMO-01) and update README to reference it.

Purpose: A compelling demo makes SOMA instantly understandable to potential users. VHS tape files are deterministic and reproducible, perfect for CI regeneration.

Output: `demo.tape` VHS script, updated README.md with demo section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-contributing-guide/12-RESEARCH.md
@.planning/phases/12-contributing-guide/12-01-SUMMARY.md
@.planning/phases/12-contributing-guide/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VHS demo tape and update README</name>
  <files>demo.tape, README.md</files>
  <read_first>
    - README.md
    - .planning/phases/12-contributing-guide/12-RESEARCH.md
  </read_first>
  <action>
    **Create `demo.tape`:**
    A VHS tape file that demonstrates SOMA's core value proposition. The tape should show:

    1. Installation: `pip install soma-ai`
    2. Setup: `soma setup --claude-code` (shows hook installation)
    3. Status check: `soma status` (shows current monitoring state)
    4. A simulated session where pressure rises (using `python -c` with soma.quickstart())
    5. Final status showing SOMA detected behavioral changes

    VHS tape settings:
    ```tape
    Output demo.gif
    Set FontSize 14
    Set Width 1200
    Set Height 600
    Set Theme "Catppuccin Mocha"
    Set Padding 20
    Set TypingSpeed 50ms
    ```

    The tape should be realistic but scripted — show actual SOMA CLI output. Use `Sleep` commands between steps for readability. Total runtime should be ~15-20 seconds of recording.

    Include comments in the tape file explaining each section.

    **Update `README.md`:**
    - Add a "Demo" section near the top (after the project description, before installation)
    - Reference the demo.gif: `![SOMA Demo](demo.gif)`
    - Add a note: "Generate the demo with `vhs demo.tape` (requires [VHS](https://github.com/charmbracelet/vhs))"
    - Keep all existing README content intact

    NOTE: Do NOT run `vhs demo.tape` — VHS may not be installed. The tape file is the deliverable; the user generates the GIF.
  </action>
  <verify>
    <automated>cd /Users/timur/projectos/SOMA && test -f demo.tape && grep -c "Output" demo.tape && grep -c "soma" demo.tape && grep "demo" README.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f demo.tape` succeeds (file exists)
    - `grep "Output demo.gif" demo.tape` finds the output directive
    - `grep "Set Theme" demo.tape` finds theme configuration
    - `grep "soma" demo.tape` finds soma commands in the tape
    - `grep -i "demo" README.md` finds demo section in README
    - `grep "demo.gif" README.md` finds the image reference
    - `grep "vhs" README.md` finds the generation instructions
  </acceptance_criteria>
  <done>VHS demo tape exists with scripted SOMA workflow, README references the demo output with generation instructions</done>
</task>

<task type="checkpoint:human-verify" gate="soft">
  <name>Task 2: Verify demo tape content and README</name>
  <files>demo.tape, README.md</files>
  <action>Human verifies demo tape content is accurate and README demo section looks correct.</action>
  <verify>
    <automated>cd /Users/timur/projectos/SOMA && test -f demo.tape && grep "demo" README.md</automated>
  </verify>
  <done>User approves demo tape content and README demo section</done>
  <what-built>VHS demo tape file and updated README with demo section</what-built>
  <how-to-verify>
    1. Read `demo.tape` and verify the scripted commands make sense for showcasing SOMA
    2. If VHS is installed (`brew install vhs`), optionally run `vhs demo.tape` to generate demo.gif
    3. Check README.md demo section looks correct
    4. Verify the demo covers: install, setup, status — the core SOMA experience
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the demo script</resume-signal>
</task>

</tasks>

<verification>
- `demo.tape` exists with valid VHS syntax
- `README.md` references demo output
- Tape demonstrates SOMA install + setup + monitoring flow
</verification>

<success_criteria>
- VHS tape file scripts a complete SOMA demo workflow
- README.md has demo section with image reference and generation instructions
- Demo content approved by user (checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/12-contributing-guide/12-03-SUMMARY.md`
</output>

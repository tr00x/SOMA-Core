---
phase: 12-contributing-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/soma/hooks/base.py
  - src/soma/hooks/cursor.py
  - src/soma/hooks/windsurf.py
  - src/soma/hooks/claude_code.py
  - src/soma/hooks/__init__.py
  - src/soma/cli/setup_claude.py
  - tests/test_hook_adapters.py
autonomous: true
requirements: [LAYER-01, HOOK-01]

must_haves:
  truths:
    - "HookAdapter protocol defines parse_input, format_output, get_event_type, platform_name"
    - "Cursor adapter correctly translates camelCase events and Cursor-specific input fields to HookInput"
    - "Windsurf adapter correctly maps split events (pre_write_code, pre_run_command, etc.) to SOMA canonical events"
    - "Claude Code adapter implements HookAdapter protocol without breaking existing behavior"
    - "soma setup --cursor generates correct .cursor/hooks.json"
    - "soma setup --windsurf generates correct .windsurf/hooks.json"
  artifacts:
    - path: "src/soma/hooks/base.py"
      provides: "HookAdapter Protocol + HookInput + HookResult dataclasses + dispatch_hook()"
      exports: ["HookAdapter", "HookInput", "HookResult", "dispatch_hook"]
    - path: "src/soma/hooks/cursor.py"
      provides: "CursorAdapter implementing HookAdapter"
      exports: ["CursorAdapter"]
    - path: "src/soma/hooks/windsurf.py"
      provides: "WindsurfAdapter implementing HookAdapter"
      exports: ["WindsurfAdapter"]
    - path: "tests/test_hook_adapters.py"
      provides: "Unit tests for all adapters"
  key_links:
    - from: "src/soma/hooks/cursor.py"
      to: "src/soma/hooks/base.py"
      via: "implements HookAdapter protocol"
      pattern: "class CursorAdapter"
    - from: "src/soma/hooks/windsurf.py"
      to: "src/soma/hooks/base.py"
      via: "implements HookAdapter protocol"
      pattern: "class WindsurfAdapter"
    - from: "src/soma/hooks/claude_code.py"
      to: "src/soma/hooks/base.py"
      via: "imports HookInput, uses dispatch_hook or keeps existing DISPATCH"
      pattern: "from soma.hooks.base import"
---

<objective>
Create the cross-platform hook adapter layer (LAYER-01) and implement Cursor/Windsurf adapters (HOOK-01).

Purpose: Transform SOMA from Claude Code-only to platform-agnostic by defining a HookAdapter protocol and implementing thin adapter layers for Cursor and Windsurf. The existing Claude Code dispatcher is refactored to conform to the same protocol.

Output: `base.py` (protocol + common types), `cursor.py`, `windsurf.py`, updated `claude_code.py`, setup commands for all platforms, and comprehensive tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-contributing-guide/12-RESEARCH.md

<interfaces>
<!-- Existing hook dispatcher pattern from claude_code.py -->
From src/soma/hooks/claude_code.py:
```python
DISPATCH = {
    "PreToolUse": pre_tool_use,
    "PostToolUse": post_tool_use,
    "Stop": stop,
    "UserPromptSubmit": notification,
    "Notification": notification,
}

def main():
    hook_type = os.environ.get("CLAUDE_HOOK", "")
    if not hook_type and len(sys.argv) > 1:
        hook_type = sys.argv[1]
    handler = DISPATCH.get(hook_type)
    ...
```

From src/soma/hooks/common.py:
```python
def read_stdin() -> dict:  # reads JSON from stdin
def get_engine():  # returns (engine, agent_id)
def save_state(engine):  # persists engine state
```

From src/soma/hooks/pre_tool_use.py:
```python
def main():
    engine, agent_id = get_engine()
    data = read_stdin()
    tool_name = data.get("tool_name", ...)
    tool_input = data.get("tool_input", {})
    # ... evaluates guidance, prints to stderr, exit(2) if blocked
```

From src/soma/cli/setup_claude.py:
```python
def run_setup_claude() -> None:  # installs hooks into .claude/settings.json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HookAdapter protocol, HookInput/HookResult types, and base dispatcher</name>
  <files>src/soma/hooks/base.py, tests/test_hook_adapters.py</files>
  <read_first>
    - src/soma/hooks/claude_code.py
    - src/soma/hooks/common.py
    - src/soma/hooks/pre_tool_use.py
    - src/soma/hooks/post_tool_use.py
    - src/soma/types.py
  </read_first>
  <behavior>
    - Test: HookAdapter is a runtime_checkable Protocol with platform_name, parse_input, format_output, get_event_type
    - Test: HookInput is a frozen dataclass with fields: tool_name (str), tool_input (dict), output (str=""), error (bool=False), session_id (str=""), file_path (str=""), duration_ms (float=0), platform (str=""), raw (dict=field(default_factory=dict))
    - Test: HookResult is a frozen dataclass with fields: allow (bool=True), message (str=""), exit_code (int=0)
    - Test: isinstance(adapter, HookAdapter) returns True for any conforming class
    - Test: dispatch_hook(adapter, raw_input) calls adapter.get_event_type, adapter.parse_input, routes to correct handler
  </behavior>
  <action>
    Create `src/soma/hooks/base.py` with:

    1. `HookInput` frozen dataclass (slots=True):
       - tool_name: str, tool_input: dict, output: str = "", error: bool = False
       - session_id: str = "", file_path: str = "", duration_ms: float = 0
       - platform: str = "", raw: dict = field(default_factory=dict)

    2. `HookResult` frozen dataclass (slots=True):
       - allow: bool = True, message: str = "", exit_code: int = 0

    3. `HookAdapter` Protocol (runtime_checkable):
       - platform_name property -> str
       - parse_input(self, raw: dict) -> HookInput
       - format_output(self, result: HookResult) -> None
       - get_event_type(self, raw: dict) -> str

    4. `dispatch_hook(adapter: HookAdapter, raw: dict) -> None` function:
       - Calls adapter.get_event_type(raw) to get canonical event ("PreToolUse", "PostToolUse", "Stop", "Notification")
       - Calls adapter.parse_input(raw) to normalize input
       - Routes to existing handlers from soma.hooks (pre_tool_use, post_tool_use, stop, notification)
       - Calls adapter.format_output(result)
       - Uses sys.exit(result.exit_code) if non-zero

    Create `tests/test_hook_adapters.py` with tests for:
    - HookInput construction with defaults
    - HookResult construction with defaults
    - Protocol isinstance check on a stub adapter
    - dispatch_hook routes correctly (mock handlers)

    Use `from __future__ import annotations` at top. Follow existing conventions (frozen dataclass, slots=True).
  </action>
  <verify>
    <automated>cd /Users/timur/projectos/SOMA && uv run pytest tests/test_hook_adapters.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class HookAdapter" src/soma/hooks/base.py` returns 1
    - `grep -c "class HookInput" src/soma/hooks/base.py` returns 1
    - `grep -c "class HookResult" src/soma/hooks/base.py` returns 1
    - `grep -c "def dispatch_hook" src/soma/hooks/base.py` returns 1
    - `grep "runtime_checkable" src/soma/hooks/base.py` finds the decorator
    - `grep "frozen=True" src/soma/hooks/base.py` appears for both dataclasses
    - `uv run pytest tests/test_hook_adapters.py -x -q` passes
  </acceptance_criteria>
  <done>HookAdapter protocol defined, HookInput/HookResult types created, dispatch_hook function works, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cursor, Windsurf, and Claude Code adapters + setup commands</name>
  <files>src/soma/hooks/cursor.py, src/soma/hooks/windsurf.py, src/soma/hooks/claude_code.py, src/soma/hooks/__init__.py, src/soma/cli/setup_claude.py, tests/test_hook_adapters.py</files>
  <read_first>
    - src/soma/hooks/base.py (just created in Task 1)
    - src/soma/hooks/claude_code.py
    - src/soma/hooks/common.py
    - src/soma/cli/setup_claude.py
    - tests/test_hook_adapters.py (just created in Task 1)
    - .planning/phases/12-contributing-guide/12-RESEARCH.md
  </read_first>
  <behavior>
    - Test: CursorAdapter.platform_name == "cursor"
    - Test: CursorAdapter.parse_input({"tool_name": "Bash", "tool_input": {"command": "ls"}, "conversation_id": "abc"}) returns HookInput with tool_name="Bash", tool_input={"command": "ls"}, session_id="abc", platform="cursor"
    - Test: CursorAdapter.get_event_type({"hook_type": "preToolUse"}) returns "PreToolUse"
    - Test: CursorAdapter.get_event_type({"hook_type": "postToolUse"}) returns "PostToolUse"
    - Test: CursorAdapter.get_event_type({"hook_type": "stop"}) returns "Stop"
    - Test: WindsurfAdapter.platform_name == "windsurf"
    - Test: WindsurfAdapter.parse_input({"tool_info": {"command": "ls"}, "agent_action_name": "pre_run_command", "trajectory_id": "xyz"}) returns HookInput with tool_name="Bash", tool_input={"command": "ls"}, session_id="xyz", platform="windsurf"
    - Test: WindsurfAdapter.get_event_type({"agent_action_name": "pre_run_command"}) returns "PreToolUse"
    - Test: WindsurfAdapter.get_event_type({"agent_action_name": "pre_write_code"}) returns "PreToolUse"
    - Test: WindsurfAdapter.get_event_type({"agent_action_name": "post_run_command"}) returns "PostToolUse"
    - Test: WindsurfAdapter.get_event_type({"agent_action_name": "post_cascade_response"}) returns "Stop"
    - Test: isinstance(CursorAdapter(), HookAdapter) is True
    - Test: isinstance(WindsurfAdapter(), HookAdapter) is True
    - Test: generate_cursor_config() returns valid JSON with preToolUse/postToolUse/stop hooks
    - Test: generate_windsurf_config() returns valid JSON with pre_run_command/pre_write_code/pre_read_code/post_* hooks
  </behavior>
  <action>
    **Create `src/soma/hooks/cursor.py`:**
    - Class `CursorAdapter` implementing HookAdapter protocol
    - `platform_name` property returns "cursor"
    - `get_event_type(raw)`: maps `raw.get("hook_type", "")` or sys.argv[1] — "preToolUse" -> "PreToolUse", "postToolUse" -> "PostToolUse", "stop" -> "Stop", "sessionStart" -> "Notification"
    - `parse_input(raw)`: returns HookInput(tool_name=raw.get("tool_name", ""), tool_input=raw.get("tool_input", {}), output=raw.get("tool_response", ""), error=raw.get("error", False), session_id=raw.get("conversation_id", ""), platform="cursor", raw=raw)
    - `format_output(result)`: writes JSON `{"user_message": result.message}` to stdout if message, also prints to stderr for compatibility
    - `generate_cursor_config() -> dict`: returns the hooks.json structure with `soma-hook PreToolUse`, `soma-hook PostToolUse`, `soma-hook Stop` commands
    - `main()` entry: creates CursorAdapter, reads stdin, calls dispatch_hook

    **Create `src/soma/hooks/windsurf.py`:**
    - Class `WindsurfAdapter` implementing HookAdapter protocol
    - `platform_name` property returns "windsurf"
    - `_WINDSURF_EVENT_MAP`: dict mapping event names to canonical types:
      - "pre_run_command" -> "PreToolUse", "pre_write_code" -> "PreToolUse", "pre_read_code" -> "PreToolUse", "pre_mcp_tool_use" -> "PreToolUse"
      - "post_run_command" -> "PostToolUse", "post_write_code" -> "PostToolUse", "post_read_code" -> "PostToolUse", "post_mcp_tool_use" -> "PostToolUse"
      - "post_cascade_response" -> "Stop"
    - `_WINDSURF_EVENT_TO_TOOL`: maps event names to tool names — "pre_run_command"/"post_run_command" -> "Bash", "pre_write_code"/"post_write_code" -> "Write", "pre_read_code"/"post_read_code" -> "Read", MCP -> "mcp"
    - `parse_input(raw)`: extracts from `raw.get("tool_info", {})`, tool_name from event-to-tool map, session_id from `raw.get("trajectory_id", "")`
    - `format_output(result)`: prints message to stderr (Windsurf reads stderr)
    - `generate_windsurf_config() -> dict`: returns hooks config with all pre/post events mapped to `soma-hook` commands
    - `main()` entry: creates WindsurfAdapter, reads stdin, calls dispatch_hook

    **Update `src/soma/hooks/claude_code.py`:**
    - Keep existing `main()` function and DISPATCH dict UNCHANGED for backward compatibility
    - Add `ClaudeCodeAdapter` class implementing HookAdapter protocol:
      - platform_name = "claude-code"
      - get_event_type: reads CLAUDE_HOOK env var or sys.argv[1]
      - parse_input: extracts tool_name from "tool_name" key, tool_input from "tool_input"
      - format_output: prints to stderr
    - Import HookAdapter, HookInput, HookResult from base
    - Do NOT change the existing main() — it works, keep backward compat

    **Update `src/soma/hooks/__init__.py`:**
    - Export HookAdapter, HookInput, HookResult, CursorAdapter, WindsurfAdapter, ClaudeCodeAdapter

    **Update `src/soma/cli/setup_claude.py`:**
    - Add `run_setup_cursor()` function that writes `.cursor/hooks.json` using `generate_cursor_config()`
    - Add `run_setup_windsurf()` function that writes `.windsurf/hooks.json` using `generate_windsurf_config()`
    - Wire into CLI: `soma setup --cursor` and `soma setup --windsurf` flags in `src/soma/cli/main.py`

    **Update `tests/test_hook_adapters.py`:**
    - Add all behavior tests listed above for CursorAdapter, WindsurfAdapter
    - Test config generation produces valid JSON with correct keys
    - Test isinstance checks for Protocol compliance
  </action>
  <verify>
    <automated>cd /Users/timur/projectos/SOMA && uv run pytest tests/test_hook_adapters.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class CursorAdapter" src/soma/hooks/cursor.py` returns 1
    - `grep -c "class WindsurfAdapter" src/soma/hooks/windsurf.py` returns 1
    - `grep -c "class ClaudeCodeAdapter" src/soma/hooks/claude_code.py` returns 1
    - `grep "generate_cursor_config" src/soma/hooks/cursor.py` finds the function
    - `grep "generate_windsurf_config" src/soma/hooks/windsurf.py` finds the function
    - `grep "pre_run_command" src/soma/hooks/windsurf.py` finds event mapping
    - `grep "preToolUse" src/soma/hooks/cursor.py` finds event mapping
    - `grep "HookAdapter" src/soma/hooks/__init__.py` finds export
    - `grep "run_setup_cursor\|run_setup_windsurf" src/soma/cli/setup_claude.py` finds both functions
    - `uv run pytest tests/test_hook_adapters.py -x -q` passes
    - `uv run pytest tests/ -x -q` full suite passes (no regressions)
  </acceptance_criteria>
  <done>Cursor and Windsurf adapters implemented, Claude Code adapter added (existing main() untouched), setup commands work for all 3 platforms, all tests pass including full suite</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_hook_adapters.py -x -q` — all adapter tests pass
- `uv run pytest tests/ -x -q` — full suite passes (no regressions)
- `python -c "from soma.hooks import HookAdapter, CursorAdapter, WindsurfAdapter; print('imports ok')"` — clean imports
- `python -c "from soma.hooks.base import HookAdapter; from soma.hooks.cursor import CursorAdapter; print(isinstance(CursorAdapter(), HookAdapter))"` — prints True
</verification>

<success_criteria>
- HookAdapter Protocol is runtime_checkable and defines the full contract
- CursorAdapter and WindsurfAdapter both pass isinstance(adapter, HookAdapter)
- Cursor adapter translates camelCase events, Windsurf adapter handles split events
- Claude Code existing behavior is completely unchanged
- Setup commands generate correct config files for each platform
- All tests pass, including full existing suite
</success_criteria>

<output>
After completion, create `.planning/phases/12-contributing-guide/12-01-SUMMARY.md`
</output>

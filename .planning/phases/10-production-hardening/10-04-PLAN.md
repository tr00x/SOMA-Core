---
phase: 10-production-hardening
plan: 04
type: execute
wave: 1
depends_on: ["10-02", "10-03"]
files_modified: [".planning/REQUIREMENTS.md"]
autonomous: false
gap_closure: true
requirements: [TEST-01, PUB-01]

must_haves:
  truths:
    - "All 5 integration tests pass with real API keys (3 Anthropic + 2 OpenAI)"
    - "soma-ai 0.5.0 is installable from PyPI via pip install soma-ai==0.5.0"
  artifacts:
    - path: "dist/soma_ai-0.5.0-py3-none-any.whl"
      provides: "Built wheel ready for upload"
    - path: "tests/test_integration_api.py"
      provides: "5 integration tests covering sync/stream/async"
  key_links:
    - from: "tests/test_integration_api.py"
      to: "src/soma/wrap.py"
      via: "soma.wrap() with real Anthropic/OpenAI clients"
      pattern: "soma\\.wrap\\("
---

<objective>
Close two human-gated verification gaps from Phase 10: run integration tests with real API keys (TEST-01) and publish 0.5.0 to PyPI (PUB-01).

Purpose: Both gaps are procedural — code is complete and correct, but human credentials are required for verification and publishing.
Output: TEST-01 and PUB-01 marked complete in REQUIREMENTS.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-production-hardening/10-VERIFICATION.md
@.planning/phases/10-production-hardening/10-02-SUMMARY.md
@.planning/phases/10-production-hardening/10-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify real API integration tests (TEST-01)</name>
  <files>tests/test_integration_api.py</files>
  <action>
    This is a human-verify checkpoint. No code changes needed — the integration tests are already written and collected.
    The executor should present the verification steps below to the user and wait for confirmation.
    After user confirms, mark TEST-01 as [x] in .planning/REQUIREMENTS.md.
  </action>
  <what-built>5 integration tests in tests/test_integration_api.py that exercise soma.wrap() with real Anthropic and OpenAI clients. Tests are guarded with skipif decorators when API keys are absent. The tests cover: sync Anthropic, streaming Anthropic, async Anthropic, sync OpenAI, and streaming OpenAI.</what-built>
  <how-to-verify>
    1. Set your API keys in the environment:
       export ANTHROPIC_API_KEY=sk-ant-...
       export OPENAI_API_KEY=sk-...

    2. Run the integration tests:
       ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY OPENAI_API_KEY=$OPENAI_API_KEY uv run python -m pytest tests/test_integration_api.py -x -v

    3. Expected: All 5 tests pass. Each test should show action_count == 1 and pressure >= 0 in the engine snapshot.

    4. If any test fails, describe the failure output so Claude can diagnose.
  </how-to-verify>
  <verify>User confirms all 5 tests pass with real API keys</verify>
  <acceptance_criteria>All 5 integration tests pass with real API credentials</acceptance_criteria>
  <resume-signal>Type "all 5 passed" or paste test output if failures occurred</resume-signal>
  <done>User confirms all 5 integration tests pass with real API keys. TEST-01 verified.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Publish soma-ai 0.5.0 to PyPI (PUB-01)</name>
  <files>dist/soma_ai-0.5.0-py3-none-any.whl</files>
  <action>
    This is a human-action checkpoint. The wheel is already built at dist/soma_ai-0.5.0-py3-none-any.whl (130KB).
    The user must run twine upload with their PyPI credentials (tr00x account). This is a one-way operation
    that cannot be automated without the user's credentials.
    After user confirms, mark PUB-01 as [x] in .planning/REQUIREMENTS.md.
  </action>
  <what-built>The wheel dist/soma_ai-0.5.0-py3-none-any.whl (130KB) has been built and verified locally. It contains all Phase 1-10 features including AuditLogger, context_usage tracking, and streaming support. The package version in pyproject.toml is 0.5.0.</what-built>
  <how-to-verify>
    1. Upload to PyPI (requires tr00x account credentials):
       uv run twine upload dist/soma_ai-0.5.0*

    2. Verify the upload in a clean environment:
       pip install soma-ai==0.5.0
       python -c "import soma; print(soma.__version__); from soma.audit import AuditLogger; print('AuditLogger OK')"

    3. Expected output:
       0.5.0
       AuditLogger OK
  </how-to-verify>
  <verify>User confirms soma-ai 0.5.0 is live on PyPI</verify>
  <acceptance_criteria>soma-ai 0.5.0 is live on PyPI and installable</acceptance_criteria>
  <resume-signal>Type "published" once twine upload succeeds and pip install confirms 0.5.0</resume-signal>
  <done>User confirms soma-ai 0.5.0 is published to PyPI and installable. PUB-01 verified.</done>
</task>

</tasks>

<verification>
After both checkpoints pass:
1. Mark TEST-01 as [x] in .planning/REQUIREMENTS.md
2. Mark PUB-01 as [x] in .planning/REQUIREMENTS.md
3. Update Phase 10 status to complete in STATE.md
</verification>

<success_criteria>
- All 5 integration tests confirmed passing with real API keys
- soma-ai 0.5.0 published to PyPI and verified installable
- TEST-01 and PUB-01 marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/10-production-hardening/10-04-SUMMARY.md`
</output>
